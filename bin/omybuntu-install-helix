#!/bin/bash

# omybuntu:summary=Install Helix and configure it to use the current Omybuntu theme

echo "Installing Helix..."
omybuntu-pkg-add helix

mkdir -p ~/.config/helix/themes

# Symlink the rendered Omybuntu theme so Helix tracks the active theme
ln -sf ~/.config/omybuntu/current/theme/helix.toml ~/.config/helix/themes/omybuntu.toml

# Only seed a config.toml if the user does not already have one
if [[ ! -f ~/.config/helix/config.toml ]]; then
  cat >~/.config/helix/config.toml <<'EOF'
theme = "omybuntu"
EOF
fi

# Ensure the symlink target exists for users whose current theme predates this template
if [[ ! -e ~/.config/omybuntu/current/theme/helix.toml ]]; then
  omybuntu-theme-refresh
fi

# Arch-based distros ship Helix as 'helix' rather than the upstream 'hx'.
if ! grep -q '^alias hx="helix"' ~/.bashrc 2>/dev/null; then
  echo 'alias hx="helix"' >>~/.bashrc
fi
